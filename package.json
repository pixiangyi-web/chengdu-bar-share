{
  "name": "chengdu-bar-share",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm ci --ignore-scripts --no-audit --no-fund",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": { "fast-xml-parser": "^5.5.6" }
}
