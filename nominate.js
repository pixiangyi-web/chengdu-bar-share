async function deviceHash(){let id=localStorage.getItem('chengdu-bar-device');if(!id){id=crypto.randomUUID();localStorage.setItem('chengdu-bar-device',id)}const bytes=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(id));return[...new Uint8Array(bytes)].map(byte=>byte.toString(16).padStart(2,'0')).join('')}
document.getElementById('nominate-form').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,button=document.getElementById('nominate-submit'),message=document.getElementById('nominate-message'),values=Object.fromEntries(new FormData(form));button.disabled=true;message.textContent='正在提交…';try{const response=await fetch('/api/nominations',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...values,device_hash:await deviceHash()})});const data=await response.json();if(!response.ok)throw new Error(data.error);form.reset();message.textContent='提名已收录，感谢你补充成都酒吧候选。'}catch(error){message.textContent=error.message||'提交失败，请稍后重试。'}finally{button.disabled=false}});
